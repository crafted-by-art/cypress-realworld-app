This file is a static HTML template for mounting components during Cypress component tests. It provides a minimal document structure with a root element (`<div data-cy-root>`) where testable components will be injected. No test suite, commands, or fixtures are present; it facilitates the environment for UI rendering. Keywords: Cypress, component testing, HTML template, mounting, root element, data-cy-root, viewport, component isolation, Cypress integration, minimal DOM.